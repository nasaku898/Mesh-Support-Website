const { makeStyles } = require("@material-ui/core");

const useStyles = makeStyles((theme) => ({
    removeBtn: {
        width: "100%"
    },

    progressBar: {
        marginTop: "10px"
    },
}))

export default useStyles